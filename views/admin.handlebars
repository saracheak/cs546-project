<!DOCTYPE html>
<html>
  <head>
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body class="admin-body">
    <header class="home-header">
      <h1>Admin Dashboard</h1>
    </header>

    <nav class="home-nav">
      <a href="/home">Home</a>
      <a href="/parks">All Parks</a>
      <a href="/editPupfile">Edit My PupFile</a>
      <a href="/biscuits">All Biscuits</a>
      <a href="/logout">Logout</a>
    </nav>

    <main>
      <section>
        <h2>Manage Parks</h2>
        <section class="admin-section">
          <h3>Pending Parks</h3>

          {{#if approveMessage}}
            <p class="success">{{approveMessage}}</p>
          {{/if}}
          {{#if approveError}}
            <p class="error">{{approveError}}</p>
          {{/if}}
          
          {{#if pendingParks.length}}
            <ul>
            {{#each pendingParks}}
              <li>
                <p>{{this.park_name}}</p>
                <br>({{this.park_type}})
                <br>{{this.address.street_1}}
                {{#if this.address.street_2}}
                <br>{{this.address.street_2}}
                 {{/if}}
                <br>{{this.address.state}}
                <br>{{this.address.zip_code}}
                <form method="post" action="/admin/parks/{{this._id}}/approve" style="display:inline;">
                  <button type="submit">Approve</button>
                </form>
                <form method="post" action="/admin/parks/{{this._id}}/deny" style="display:inline;">
                  <button type="submit">Deny</button>
                </form>
              </li>
            {{/each}}
            </ul>
          {{else}}
            <p>No pending parks to approve.</p>
          {{/if}}
        </section>
      </section>
      <section>
        <h2>Manage Biscuits</h2>
        {{!-- CREATE biscuit: POST /admin/biscuits --}}
        <section class="admin-section">
          <h3>Create New Biscuit</h3>

        {{#if createMessage}}
          <p class="success">{{createMessage}}</p>
        {{/if}}
        {{#if createError}}
          <p class="error">{{createError}}</p>
        {{/if}}

          <p>Use this form to create a new biscuit. The name and/or description cannot be the same as an existing biscuit</p>

          <form method="post" action="/admin/biscuits" class="admin-form">
            <div>
              <label for="biscuit_name">Biscuit Name:</label>
              <input
                type="text"
                id="biscuit_name"
                name="biscuit_name"
                required
              />
            </div>

            <div>
              <label for="description">Description:</label>
              <textarea
                id="description"
                name="description"
                required
              ></textarea>
            </div>

            <button type="submit">Create Biscuit</button>
          </form>
        </section>

        {{!-- UPDATE biscuit: POST /admin/biscuits/update --}}
        <section class="admin-section">
          <h3>Update Biscuit</h3>

        {{#if updateMessage}}
          <p class="success">{{updateMessage}}</p>
        {{/if}}
        {{#if updateError}}
          <p class="error">{{updateError}}</p>
        {{/if}}

          <p>
            Use this form to update an existing biscuit. <br><br> Enter the <code>biscuitId</code> of the biscuit you want to update.
            Both fields are required. 
            <br>If you only want to update one field, like the description, then you should enter the same 
            biscuit name with a new description. 
            <br> NOTE: If you update a biscuit name, it must be fix it in the data/biscuits.js logic. If you create a biscuit the autoaward 
            logic also needs to be created before it will work. 
          </p>

          <form method="post" action="/admin/biscuits/update" class="admin-form">
            <div>
              <label for="biscuit_id">Biscuit ID:</label>
              <input
                type="text"
                id="biscuit_id"
                name="biscuitId"
                required
              />
            </div>

            <div>
              <label for="new_name">New Name:</label>
              <input type="text" name="biscuit_name" id="new_name" />
            </div>

            <div>
              <label for="new_desc">New Description:</label>
              <textarea name="description" id="new_desc"></textarea>
            </div>

            <button type="submit">Update Biscuit</button>
          </form>
        </section>

        {{!-- DELETE biscuit via POST /admin/biscuits/delete --}}
        <section class="admin-section">
        <h3>Delete Biscuit</h3>

        {{#if deleteMessage}}
            <p class="success">{{deleteMessage}}</p>
        {{/if}}

        {{#if deleteError}}
            <p class="error">{{deleteError}}</p>
        {{/if}}

        <p>Enter the biscuitId of the biscuit you want to delete.</p>

        <form method="post" action="/admin/biscuits/delete" class="admin-form">
            <div>
            <label for="delete_biscuit_id">Biscuit ID:</label>
            <input
                type="text"
                id="delete_biscuit_id"
                name="biscuitId"
                required
            />
            </div>
            <button type="submit">Delete Biscuit</button>
        </form>
        </section>
      </section>
    </main>
  </body>
</html>
